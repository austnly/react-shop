import styles from "./Footer.module.scss";

const Footer = () => {
	return (
		<div className={styles.Footer}>
			<p>Built by Austin in ReactJS & Bootstrap.</p>
		</div>
	);
};

export default Footer;
